#!/usr/bin/expect -f
set timeout 60
set password ")l2fyDHz60u,nTAd,@tD"

spawn ssh root@148.230.88.203 "ls -la /var/www/coa-viewer/backend"
expect {
    "password:" { send "$password\r" }
    "yes/no" { send "yes\r"; exp_continue }
}
expect eof
